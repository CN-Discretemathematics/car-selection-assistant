fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sqlalchemy>=2.0,<3.0
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
python-dotenv>=1.0
# 生产数据库驱动（postgresql+psycopg）
psycopg[binary]>=3.1
# 云 Redis（会话/限流/验证码生产存储）
redis>=5.0
# 阿里云 OSS（网页快照/图片原始文件）
oss2>=2.18
crcmod>=1.7
pycryptodome>=3.20
# RAG 编排（app/rag：LangGraph 查询/摄取流水线，配套 langchain-core、langsmith 自动引入）
langgraph>=1.2,<2
# 中文整词分词（BM25 稀疏路 + 车圈领域词表；缺失时自动回退字符二元组）
jieba>=0.42
# 测试
pytest>=8.0
fakeredis>=2.20
